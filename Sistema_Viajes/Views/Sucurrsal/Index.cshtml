@model IEnumerable<Sistema_Viajes.Models.Sucurrsal>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FSucurrsal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SUbicacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FSucurrsal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SUbicacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdSucurrsal">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdSucurrsal">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdSucurrsal">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
