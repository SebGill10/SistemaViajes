@model IEnumerable<Sistema_Viajes.Models.SucursalColaborador>

@{
    ViewData["Title"] = "Administrar sucursales";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Administrar sucursales</h1>

<p>
    <a class="btn btn-outline-success" asp-action="Create">Crear nuevo puesto</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Distancia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sucursal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Colaborador)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Distancia)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sucursal.FSucursal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Colaborador.Apellido)
            </td>
            <td>
                @Html.ActionLink("Edit", "Editar", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Informació", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Eliminar", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
